FOC Web -- outstanding work
===========================

Everything here is verified against the map, not speculative. Counts are
measured, not estimated.

EFFECTS (closing out the effects pass)
--------------------------------------
[x] Fractional alpha on geoset visibility  (FIXED)
    Geoset visibility was binary -- render.js did `mesh.visible = a > 0.05`.
    Warcraft III draws the geoset at the track's own alpha, so anything meant to
    fade popped instead. Now the alpha reaches the material, and it is
    multiplied into the authored opacity rather than replacing it, so a geoset
    animation and a SetUnitVertexColor compose (0.5 x 0.5 = 0.25) instead of the
    last writer winning.
    18 of the 1105 converted models carry an alpha strictly between clear and
    solid: the five auras, War Stomp's and Thunder Clap's caster rings, four
    missiles, the three feathers, green smoke, the goblin sapper and the sand
    wave. Trueshot Aura's ring is the plainest case -- authored to run
    0.30 -> 0.535 -> 0.30 and never solid at any point, so it had been drawn at
    full opacity for its entire life.
    A model that never fades still pays nothing: materials are only cloned when
    a geoset is actually part-way through one.
    Found while fixing it: two of the eighteen -- Devotion Aura and the Voodoo
    Aura -- have no glTF animation at all, because nothing on them moves and the
    geoset alpha *is* the animation. No clip meant no mixer, no mixer meant no
    time to sample, so they were frozen on their first frame, which for both is
    fully transparent. They now get a clock of their own from the sequence's
    duration. tools/geoset_test.mjs, 15 checks.

[x] Corpses vanish instead of dissolving  (FIXED)
    Reported from play. Warcraft III fades a body out over Decay Flesh
    and Decay Bone through the *material's* alpha track (KMTA) -- a different
    track from the geoset alpha above. mdx.py parsed it and mdx2gltf.py threw it
    away, so the client had nothing to fade with and a corpse held full opacity
    until its timer ran out.
    Now exported per sequence off the layer pick_layer chose, so it is the alpha
    of the surface actually drawn rather than of a team-colour underlay, and
    multiplied into the same composition as the geoset track and any tint.
    Worth keeping the scale honest: of 90 model/sequence pairs whose material
    fades to clear, only 25 do it while the geoset wearing it is still drawn --
    those are the corpses this visibly dissolves. In the other 65 the geoset
    alpha has already culled the mesh by then, and there this changes nothing on
    screen. 133 of 250 sampled unit models animate the track at all.
    Found while fixing it: Trueshot Aura animates *both* tracks, and geoset_test
    was asserting a raw opacity that only accounted for one. Its ring is
    authored at 0.534 geoset x 0.8 material = 0.427, which is what the game
    draws; the test now asserts the geoset factor it is actually about, plus the
    product. tools/dissolve_test.mjs, 9 checks.

EFFECTS (cont.)
---------------
[x] Buff art -- no buff ever shows its attached model  (FIXED)
    Warcraft III gives a buff a Target Attachment model and an attachment point
    (w3h fields ftat / fta0). No buff in the map has ever drawn its art.
    Visible example: Ichigo's Hollow form (B001) and Vizard form (B000) both
    attach Abilities\Spells\Undead\DeathCoil\DeathCoilMissile.mdl at "head".
    That missing orb is the unchanged-model half of the report.

    Measured 2026-08-27, and the old note here was wrong twice. It said "nothing
    reads w3h" -- abilities.py already did -- and it called the job wiring, on a
    count of 19 models. The real shape, layer by layer:
      w3h buff art .................. 149 buffs carry a target model
      distinct models ............... 88, of which 86 are converted; the two
                                      that are not (SleepSpecialArt, WebTarget)
                                      are absent from the archives entirely,
                                      like the stale rows in the sound tables
      ability -> buff id ............ ALREADY COMPILED: 545 of 1046 abilities
                                      carry one per level, 87 of them naming a
                                      buff that has art
      attachment points ............. DONE now -- fta0..N and ftac compiled, and
                                      ftat split on commas, because it is a
                                      model *list*, one per point (Thorny Shield
                                      hangs four, Bloodlust two). 11 buffs name
                                      a point; 9 of those are reachable in play
      server applies a buff by id ... MISSING. Across 130 casts the only buff
                                      ever applied is an anonymous 'stun', 9x,
                                      from the hand-written approximations in
                                      abilities.js. Nothing ever says "unit U
                                      now has B000"
      buffs serialised to a client .. MISSING
      client attach/detach .......... MISSING, but the renderer already resolves
                                      attachment points and AddSpecialEffectTarget
                                      already attaches the map's own 59 effects
    DONE. The middle turned out to be half-built already: the AUfa/AUfu/ACfa
    case in abilities.js was the one applyBuff call carrying `code: i.buff`, and
    it is exactly the path this map uses -- it casts Frost Armor off a dummy
    purely to stamp a named buff. So only two pieces were missing, and both are
    in: the snapshot now carries the codes of buffs that draw something (and
    only those -- a snapshot goes to everyone fifteen times a second), and
    render.js syncBuffArt hangs the models through the same route
    AddSpecialEffectTarget takes, so a buff model gets its emitters, ribbons and
    texture animation like any other effect.
    Ichigo's mask lands on Head_Ref, eight levels into the skeleton rather than
    on the root, and comes down when the buff ends. removeView names the buff
    effects explicitly: they hang off a bone, so the existing sweep for effects
    parented to v.root could not see them.
    tools/buffart_test.mjs, 11 checks, driven through the real cast rather than
    through applyBuff -- the lesson from the hero-death warning, which passed
    every check while the game stayed silent.

[~] The ground meets the cliffs badly  (HALF FIXED)
    Reported from play: at the seam between a cliff's floor and the
    baked ground the two are visibly different resolutions and read as unblended.
    Two causes found, both fixed:
      - bake_ground.py baked at PX=32 while Blizzard's own tile variations are
        64x64, so every ground tile was resampled to half size before it was
        ever drawn. Now PX=64, the source art's own resolution: _ground.png goes
        3072x2048 -> 6144x4096 (2.1 MB -> 8.3 MB), 0.25 -> 0.50 texels per world
        unit. Going higher would only upscale -- more bytes, no more detail.
      - the cliff textures ask for max anisotropy and the ground never did. At
        an RTS camera angle the ground is seen edge-on, so it smeared while the
        cliff beside it stayed sharp. It asks for the same now.
    STILL OPEN: the cliff surface carries 256 texels over a single 128-unit
    cell -- 2.0 per unit against the ground's 0.50 -- so a 4x mismatch remains,
    and matching it would mean baking at 4x the pixels from 64px source art,
    which is upscaling, not detail. Whether that is worth it is a judgement
    call and wants a look at the real thing first.

[ ] Ramp transitions: the CliffTrans wedges are never placed
    Also reported from play -- the transition art at a cliff edge is absent.
    CliffTypes.slk gives every cliff type a *second* model directory --
    `rampModelDir`, CityCliffTrans for this map's CYsq -- holding the wedges
    Warcraft III sets where a ramp meets a cliff. cliffs.py reads
    cliffModelDir only, so those cells get ordinary cliff walls and the ramp
    mouth has a hard edge instead of a carried-down ground.
    What is established, so the next attempt starts here rather than from
    scratch:
      - the art exists: 16 CityCliffTrans models, and Models.path() already
        builds exactly the right filename for them
      - their codes letter a ramped corner H or L by which layer it sits on
        rather than A/B/C, which is why every one of the 16 has exactly one H
        and one L
      - this map has 4 fully-ramped cells and 20 cliff cells touching them
    What is NOT established, and where an attempt failed: which cells take a
    wedge. Coding every cell whose ramp-flagged corners span both layers gives
    12 cells, and all 12 codes have art -- but the resulting mesh fails
    cliff_test with 64 height mismatches (max 128, exactly one layer) and 8
    cells covered that were never declared, and some of the 12 sit three rows
    from the ramp mouth. So the corner-to-letter rule is close but the
    selection or the base layer is wrong, and it wants Blizzard's actual
    placement rule rather than another inference. Reverted; the tree is green.

INTERFACE (the Warcraft III console)
------------------------------------
The frame, the slots, the selection circles and the health bars are in. What
Warcraft III's own screenshot still has and we do not:

[x] The camera does not know the console takes 29% of the screen  (FIXED)
    The world is drawn across the whole window and the console covers the bottom
    of it, so the middle of the window is not the middle of what can be seen.
    The camera aimed at the window centre, 0.5, when the centre of the visible
    strip is 0.3533 -- 14.7% of the screen too low, which crowded the hero
    against the frame and wasted the room above him.

    The projection is shifted rather than shrunk, which is what the game does:
    the world still fills the screen and the console overlays it, so nothing is
    cropped and only the optical centre moves (camera.setViewOffset). The
    fraction is read off the console pieces themselves -- the tallest
    BOTTOM-anchored tile, 0.2933 for this layout, which is ConsoleUI.fdf's 0.176
    of the game's 0.6-tall screen box -- rather than written into the client.

    The part that could have broken quietly: raycasting has to read the same
    shifted projection or every click lands 14.7% of the screen from the cursor.
    It does -- three's setFromCamera goes through projectionMatrixInverse -- and
    the test asserts it by projecting each visible unit to its own screen point
    and picking it back, 15 of 15.
    tools/camfit_test.mjs, 6 checks.

[x] The top strip is drawn and empty  (FIXED)
    Warcraft III hangs Quests/Menu/Allies/Chat off the left of it and gold,
    lumber and supply off the right. Both are frames ConsoleUI.fdf never
    mentions, because the game anchors them to the screen rather than to the
    console -- which is why the strip art was drawn and its contents were not.
    uiframe.py now reads ResourceBar.fdf and UpperButtonBar.fdf alongside it.

    Two indirections had to be followed rather than guessed. A button's
    NormalTexture names a Texture *block* in the same file, not a skin alias:
    the block carries the alias in File and the slice of the sheet in TexCoord,
    and all four button states are bands of one buttonstates texture, so both
    have to be resolved or the button draws the whole sheet. And every readout
    is anchored to the resource frame's TOPRIGHT, not the screen's, so the
    offsets only mean anything inside a box of the frame's own width.

    Blizzard commented out the upkeep *icon* in their own ResourceBar.fdf and
    left the text; that is reproduced rather than corrected, so upkeep is the
    one item that resolves to no texture.

    The numbers are the server's: the map spends gold (27 references in
    war3map.j) and lumber (8) and never touches food, so supply reads what the
    engine reports for it, which is nothing -- the same as the game would show.

    Quests and Menu are drawn from the layout's own DisabledTexture and grey
    disabled colour. The engine's QuestSetTitle and QuestSetDescription are
    no-ops, so there is no quest text to show, and a button that silently does
    nothing is worse than one the game itself would grey out. Allies opens the
    scoreboard, which is the one thing already behind it.

    Our own centre score strip sat where the resource bar goes; the resource bar
    is the game's layout, so ours is what moved down.
    tools/topbar_test.mjs, 11 checks.

[x] The command card's fourth column is clipped  (FIXED)
    ConsoleUI.fdf ends Tile03 partway through the last column and the 0.04-wide
    Tile04 carries the rest -- the same seam that cuts the portrait arch in half,
    one axis over. Measured tile by tile, Tile03's fourth column came out 0.080
    of the tile against ~0.155 for the other three and ran off its right edge, so
    the card drew three full columns and a clipped one at 37px against 71px.

    seamColumn() generalises seamArch to it. Tile04's own openings supply the
    missing half: it resolves one per row for the rows whose art is unambiguous,
    and since the column is vertical, the horizontal extent those rows agree on
    is the column's -- a row Tile04 did not resolve takes the same one rather
    than a guessed width. Both tiles are BOTTOMRIGHT, so it is all measured as a
    distance from the right edge of the screen.

    The joined width comes to 0.04935 of the screen against 0.0496 for the other
    three columns, which is the check that the geometry is right rather than
    merely plausible. All twelve cells now measure 71-72px on an even 78.3px
    pitch.
    tools/card_test.mjs, 7 checks, including that the portrait arch did not
    regress while its helper was generalised.

[x] Missing textures at the map's two city gates  (FIXED)
    Reported from play: the doors at the top left and top right drew flat white
    slabs where their pillars should be.

    Not a 404 -- nothing failed to load. CityEnteranceGate.mdx has two geosets:
    one takes Textures\\CityStructures.blp and draws the bars and beams, the
    other takes texture 4, which is replaceableId 11 with an empty path.
    Warcraft III's slot 11 is "this tileset's cliff texture", the same slot the
    cliff meshes use. mdx2gltf.py's tex_uri handled ids 1 and 2 (team colour and
    glow) and returned None for everything else with no path, so the material
    got no texture and drew white.

    tex_uri now resolves 11 the way tools/cliffs.py resolves it for the terrain:
    TerrainArt\\CliffTypes.slk names the texture unqualified and every tileset
    bar Lordaeron Summer ships its own recolour as <tileset>_<texFile>.blp.
    Where a map declares more than one cliff type it takes the one the terrain
    actually uses -- this map lists CYdi and CYsq, but cliffTex is CYsq on 4321
    vertices against CYdi on 37, and taking the first entry instead gave the
    gates Cliff0, a cliff that appears almost nowhere here. It now resolves to
    Y_Cliff1.png, the same texture the cliff mesh draws.
    Exactly 2 placements, which is the two gates. tools/doodadtex_test.mjs.

[x] 27 tree destructables drew nothing at all  (FIXED)
    Reported from play as the trees in the top left not rendering; found first
    by doodadtex_test while fixing the gates. 17 LTlt (LordaeronTree) in the
    top-left quadrant and 10 BTtw (BarrensTree) in the top-right. Not white but
    invisible: their one material is filterMode transparent, so with no texture
    it sampled nothing and every fragment went.

    Same root cause as the gates -- a replaceable slot with no path -- but the
    texture belongs to the destructable *type*, not the model, so the gates' fix
    could not extend to it. Units\\DestructableData.slk carries texID and
    texFile per type:
      LTlt  31  ReplaceableTextures\\LordaeronTree\\LordaeronSummerTree
      BTtw  33  ReplaceableTextures\\BarrensTree\\BarrensTree
      YTcx  11  ReplaceableTextures\\Cliff\\Cliff1.tga
    146 destructables name a texID, and 32 of the 52 models carrying one are
    shared by types that disagree -- lordaerontree alone is used by five
    destructables with five different tree textures.

    Three things were missing, one per stage:
      - extraction never pulled the art. grab_model follows the textures a
        model's TEXS chunk names and a replaceable slot names none, so the BLPs
        sat in war3.mpq untouched. extract_blizzard now follows texFile for
        every placed type; 3 textures on this map.
      - stage.py read DestructableData for file, defScale and Name and dropped
        texFile. It now resolves it to the converted PNG and puts it on the
        doodad type.
      - the client had nothing to apply. addDoodads now does, and only to a
        material with no texture -- the gate's cliff slot is already resolved by
        mdx2gltf to the tileset's own recolour (Y_Cliff1), which is sharper than
        the bare Cliff1 this table names, and overwriting it would undo that.

    The materials are cloned and the clone shared by every instance of the type:
    skeletonClone hands out the model cache's own materials, so writing to one
    would repaint every doodad drawn from that model and every one after it --
    the same trap the unit tinting fell into.
    407 doodad meshes now, every one textured, none without.
    tools/doodadtex_test.mjs, 8 checks. The KNOWN entry that carried this is
    gone, which is how run_tests says an entry there should be retired.

[x] Gates drew their closed doors and their own rubble at once  (FIXED)
    Reported from play. A destructable model carries its wreckage as extra
    geosets and hides them with a geoset-alpha (KGAO) track, so one model serves
    both states: the elven gate stands as [1,0,1,0] and dies as [1,1,0,0], which
    makes geoset 1 the debris and 2 the gate. Four of the five gate models do
    this -- 3 to 5 tracks each -- and the city entrance gate has none at all,
    which is why that one alone looked right.

    Units sample those tracks through play(). A doodad is never animated, so
    nothing sampled them and every geoset drew at full alpha.

    addDoodads now applies the model's own "stand" geosetAlpha when it builds
    one, hiding what that sequence hides. Visibility is per-object, so it is set
    on the clone and never touches the material the model cache holds -- the trap
    the unit tinting fell into. 35 of the 407 doodad meshes are wreckage.

    Two things measured on the way, both of which killed a wrong first guess:
      - three's Box3 reads the bind-pose vertex buffer and ignores skinning, so
        comparing bounding boxes said posing changed nothing. Rendering the two
        and diffing the pixels said the same, correctly: the bind pose *is* the
        stand pose and no bone moves. It is the alpha track, not the skeleton.
      - the glTF mesh order matches meta.geosets exactly -- checked by comparing
        triangle counts element for element across all four models -- so a plain
        traverse indexes the alpha array correctly.
    tools/doodadtex_test.mjs, 11 checks.

[x] Walls and gates were walked straight through  (FIXED)
    Reported from play. war3map.wpm is the *terrain's* pathing and the World
    Editor bakes ordinary doodads into it -- the 204 city low walls read 202,
    no-walk, straight from the file. Destructables are not baked in, because
    Warcraft III stamps their footprint at runtime and lifts it when they die,
    so the 58 stone walls, all six gates and the 27 trees read 64 or 72, which
    is walkable. 64 of 64 wall and gate centres were open.

    The footprint is a TGA named per type in pathTex, one pixel per 32-unit
    pathing cell -- the same grid war3map.wpm uses -- with the flags in the
    colour channels: red unwalkable, green unflyable, blue unbuildable. Only red
    is read; nothing here flies and nothing builds. Read off the art rather than
    assumed: StoneWall1Path is a 10x2 solid bar (320x64 units), StoneWall3Path
    the same rotated, GateLarge1Path a 28x4 bar, and Gate2Path a 22x22 diagonal
    band, which is what the four diagonal gates need.

    Two stages were missing. extract_blizzard pulled the models a placed type
    names and not its pathTex, so 20 of the 22 footprints were never unpacked;
    it follows pathTex now. stage.py then stamps them into walk.bin at each
    placement, taking the rotation to the nearest quarter turn because Warcraft
    III keeps a stamped footprint square to the grid.

    361 placements stamped. The footprints went in a quarter turn off and this
    only half worked -- see the next item.

    Found while testing: camfit_test asserted a click always picks the unit it
    was aimed at, and the creeps now path differently around the walls, so two
    overlapped and the pick correctly returned the nearer one. The assertion was
    wrong, not the pick; it now accepts a unit drawn in front.

    STILL OPEN: the gates block permanently. Warcraft III lifts the footprint
    when a destructable dies, and nothing here can die yet -- see the
    destructables item below.

[x] The stamped footprints were a quarter turn off  (FIXED)
    Reported from play: heroes still walked through. Every wall and gate on this
    map is placed at facing 270 -- 30 LTw0, 23 LTw2, all six gates -- and 270 is
    the World Editor's default facing, so the path textures are authored in that
    orientation. The quarter turn was measured from zero instead, which turned
    every footprint 90 degrees: StoneWall3Path is a 2x10 bar for a wall running
    north-south and it went in as a 10x2 bar running east-west, through the
    wall's own middle. That blocks the centre and leaves an eight-cell hole to
    either side, and the walls sit 10 cells apart, so a line of wall stamped as
    a row of detached stubs.

    The test passed the whole time because it asserted that each placement's
    centre cell was blocked, which was true throughout. The check that fails is
    measured: take the first open cell either side of each placement, skip the
    pairs that are not separated at all, and path between the rest. 17 of 112
    crossings had a detour under 1.2x -- straight through. Measuring the turn
    from 270 gives 0 of 63 under 3x.

    The blocked-cell count *falls*, 2456 to 2348, and that is the confirmation
    rather than a worry: a bar aligned with its wall overlaps its neighbours, so
    it claims fewer unique cells while actually forming a barrier.

    With the walls continuous the six closed gates now seal what is behind them:
    49724 cells reachable with their footprints lifted against 30204 with them
    shut. The walls themselves seal nothing -- 96.9% of what the bare terrain
    reaches -- and the fountain and all 28 shops stay reachable with every gate
    closed. What is cut off is the creep ground, 51 map-placed creeps. That is
    what a closed gate is for: war3map.j never touches a destructable, no
    KillDestructable and no DestructableRestoreLife anywhere in it, so the only
    thing that opens one is destroying it.

    stage.py now also writes public/data/pathstamp.json, the cells each of the
    361 placements claims, kept per placement rather than merged because
    neighbouring walls share cells where their bars overlap -- a cell may only
    reopen once nothing left standing still claims it. That is what lifting a
    footprint on death needs, and the test uses it to flood the map with the
    gates open. tools/pathblock_test.mjs, 11 checks.

[x] Destructables were not entities  (FIXED)
    Nothing could target or damage a gate: no destructable existed server-side
    at all, so a closed gate blocked for ever. All 115 the map places are
    entities now, because every one of them owns cells that have to be released
    if it dies, but only the six gates can be clicked: DestructableData's own
    selectable flag is 1 for them and 0 for the 58 stone walls, the 27 trees and
    the 24 pathing blockers. They are 500 hit points at war3map.doo's life 100 --
    shut and whole.

    The map's script has no part in this. war3map.j names five destructable
    functions and every one of them is dead Blizzard.j boilerplate -- the
    filterEnumDestructablesInCircleBJ global and its filter. No KillDestructable,
    no DestructableRestoreLife, no enumeration. A gate opens because a player
    hit it, and for no other reason.

    Two things the data settled that would otherwise have been guesses:

      - pathTexDeath. A broken gate is not clear ground: the death footprint is
        its two posts. Gate2PathDeath is 32 red pixels at the ends of the 124
        the live one covers, so breaking a gate opens 92 cells and leaves 32 as
        rubble. Those textures had never been unpacked -- extract_blizzard
        followed pathTex and not pathTexDeath, the same gap that left the live
        footprints missing.
      - the range to a gate is measured to its footprint, not to its centre. A
        gate is 896 units across and its centre is inside its own footprint,
        where nothing can stand; measured to the centre a melee hero pressed
        against one is over 400 units away from it and never swings.

    A cell reopens only when nothing standing still claims it -- neighbouring
    walls share cells where their bars meet -- and only if the bare terrain
    allowed it in the first place, which is why stage.py records the
    terrain-allowed subset per placement rather than the whole footprint.

    World keeps its own copy of walk.bin rather than the module's array: a gate
    broken in one match must not start the next one broken.
    tools/destructable_test.mjs, 27 checks, including a hero ordered at a gate
    from 460 units away walking to it and breaking it, and the crossing it seals
    going from no route at all to 352 units.

    The gates animate as they are hit and as they fall. Every gate model carries
    a Stand Hit sequence -- three of the elven gate's fourteen bones take ten
    rotation keyframes in it, and the geoset alphas match the stand pose, so it
    is the doors shuddering and not a swap of what is drawn. Doodads never
    animated at all here; the six that can be struck now get a mixer and the
    other 357 stay static clones.

    Death is not an instant swap either. The death sequences carry a geoset
    alpha *curve* as well as held values -- the elven gate curves geoset 3 from
    0 up to 1 across the first two thirds of the clip -- and reading only the
    held alphas left that geoset hidden for good. It is ticked against the clip
    now. Visibility rather than opacity: a doodad's materials are shared by
    every placement of its type, which is what keeps a replaceable texture from
    being cloned 27 times for the trees, so fading one instance would fade them
    all. Four of the six gates gain a geoset as the clip runs; the two city
    entrance gates have no geoset animation at all and are left standing rather
    than hidden, which would leave a hole in the wall.

    NOT DONE: deathSnd names a label (TreeWallDeath) and animsounds.json is keyed
    by MDX event id, so there is nothing to resolve it against. Only the trees
    carry one and nothing can hit a tree here. The field is compiled and carried
    so it is ready when a label-keyed table exists.

[x] Two test checks that could not fail  (FIXED)
    Found while running the suite for the destructable work, not looked for.

    run_tests reuses a server already answering on the port, and one had been up
    since a previous session. Every browser test in that run was measuring code
    from before the change. It is worth knowing before reading a green run.

    render_test's GPU-memory checks then failed for real once they had a current
    server, and both were wrong rather than the code being wrong:
      - the counters were read off a lost WebGL context. Chromium drops this
        page's first context and hands back another a few frames later, and
        three no-ops its renders in between, so every reading was 0 and
        "peak > base" compared 0 with 0. It now draws across frames until the
        counter moves.
      - the baseline was taken before the model cache was warm. loadModel keeps
        the glTF it parsed, so the first spawn of a type uploads geometry that is
        never given back -- the cache doing its job -- and "every cycle gives the
        geometry back" was comparing against an empty renderer. Warming it first
        gives 29 -> 55 -> 29, the same across three runs.

[x] Orochimaru's revive did nothing  (FIXED)
    Reported from play. A01K, his level-10 skill -- one level, no hotkey,
    origin AOre -- is Reincarnation, and its entire trigger in war3map.j is one
    line: PlaySoundBJ on EVENT_PLAYER_UNIT_SPELL_EFFECT. In Warcraft III the
    revive is the engine's, not the map's, and nothing in server/ implemented
    AOre, so the death path never looked for it and he died like anyone else.

    The numbers are the ability's own. AbilityData gives A01K DataA 3 and Cool
    120 against Blizzard's AOre at 7 and 240, and AbilityMetaData gives the Ore1
    field to exactly AOre, ACrn and ANrn -- "Reincarnation Delay" in
    WorldEditStrings. So: he dies, the death events fire and the kill is still
    paid, and three seconds later the body stands up where it fell with
    everything back. The cooldown starts when the ability fires and not when he
    rises, so a second death inside two minutes is the end of him.

    Found while writing it: reviveUnit had no already-alive guard, and the map
    revives its heroes at their altar on a timer (ReviveHero/ReviveHeroLoc, 10
    calls each). ReviveHero on a living hero does nothing in Warcraft III; here
    it would have dragged the reincarnated hero to the altar and healed him a
    second time. It now returns false and leaves him alone.
    tools/reincarnate_test.mjs, 17 checks.

[x] pickDoodad missed anything that had not been rendered yet  (FIXED)
    Raycasting reads matrixWorld and does not compute it, and addDoodads adds
    its group to the scene only once every type has loaded, so a gate clicked
    before that first render was still carrying an identity matrix and sat at
    the origin as far as the ray was concerned. It showed up as a flaky test and
    was a real defect: the fix is one updateWorldMatrix before the cast.

[ ] The info panel's bottom row clips against its arch
    GOLD and K/D run out of the opening at 1440x900.

GAMEPLAY / INPUT
----------------
[x] Senbonzakura spawned nothing and dealt its petal count as damage  (FIXED,
    except the petals' burn -- see NOT DONE)
    Researched 2026-08-30; every number below re-verified against the files
    before being written here, because the audit entry this replaces was wrong
    about the central one.

    A00T 시해:천본앵, Byakuya H00N, slot W, ten levels from hero level 5 every
    four levels, no mana cost at any rank. Base AUls, Crypt Lord's Locust
    Swarm: N flying units are released from the caster over time, auto-attack
    what is near, and return a fraction of their damage to him as life.

    AbilityMetaData's Uls1..Uls5 resolve through WorldEditStrings to:
      Uls1 data1  Number of Swarm Units        50 (rank 1-5) / 100 (6-10)
      Uls2 data2  Unit Release Interval        0.05 s
      Uls3 data3  Max Swarm Units Per Target   12
      Uls4 data4  Damage Return Factor         0.1
      Uls5 data5  Damage Return Threshold      20
      Ulsu unit   Swarm Unit Type              uloc (1-5) / u000 (6-10)
    with area 800/1000 and duration 5-25 s / 20-60 s.

    AbilityAudit.txt read data3 as the petal count. It is the per-target cap;
    corrected there. The live defect is worse than a missing animation:
    abilities.js's AUls case passes d1 to w.damage, and d1 is Uls1, so casting
    it deals one instant invisible pulse of 50 or 100 -- the petal count used
    as a damage figure. The data-driven summon path (summonFrom, which already
    reads count from data1 and the type from `unit`) is never reached because
    the case pre-empts it.

    The petals are fully present and need no invention: uloc and u000 are the
    stock Blizzard locust with the map's edits, and the only differences
    between them are attack damage (150 vs 600), the immolation they carry,
    the name and the race. Both are Aloc, neither is invulnerable, both have a
    real model and icon already converted.

    NOT ESTABLISHED, and it is the number that decides the spell's whole
    output: the tick interval of the petals' immolation (A02H 150 on uloc,
    A02G 200 on u000, both radius 500). Apig's three fields are Damage per
    Interval, Mana Drained per Second and Buffer Mana Required -- no period
    among them. Blizzard's own tooltips call the field "damage per second",
    which fits a hard-coded 1 s; but every retail immolation also carries
    Dur = 1, which fits Dur being the interval just as well, and this map
    overrode Dur to 10 on both petals. The two readings differ by 10x on 50 to
    100 petals at once. Nothing in AbilityMetaData, WorldEditStrings, the
    strings files or the map's own tooltips discriminates. Also unstated: the
    life-return arithmetic behind Uls4/Uls5, and the petals' target-selection
    rule beyond the 12 cap.
    So the swarm was built exactly and the burn was not.

    DONE: the AUls case now releases Uls1 petals of type Ulsu, one every Uls2
    seconds, all expiring when the duration runs out, and the damage is the
    petals' own attack out of their unit type -- no damage figure of ours
    anywhere in it. Measured: 50 petals released over 2.43s against the 2.50s
    the data implies, gone by the end of the 5s. releaseSwarm/stepSwarms in
    world.js, beside the existing channel scheduler.
    tools/swarm_test.mjs, 18 checks.

    ONE APPROXIMATION, marked in the code at world.js isLocust: Warcraft III's
    Locust makes a unit untargetable, and the extracted data does not say so --
    Aloc has Name=Locust in CommonAbilityStrings and no Func entry, no tooltip
    and no data fields. Without it, fifty petals are fifty valid targets and
    every hero in the lane abandons what it was hitting to swing at them, which
    is plainly not the map's behaviour either. So locusts are excluded from
    auto-attack acquisition and from nothing else -- not from area damage,
    because whether Warcraft III exempts them there is a second question the
    files also do not answer.

    NOT DONE: the petals' immolation (A02G/A02H), for the interval reason
    above, and the Uls4/Uls5 life return, whose arithmetic is stated nowhere.
    Both leave the swarm quieter than the real spell rather than wrong.
    Worth watching: 50-100 extra units per cast, against a frame that already
    costs 45 ms at 307 units. Not measured under load yet.

[x] On-attack procs  (FIXED for two of the four bases; the other two cannot
    be built from the data -- see below)
    Researched 2026-08-30. ACct, ANdb, Alit and Aspo account for 10 of the
    audit's 13 dead abilities, and the plan of "one on-attack hook covers all
    four" does not survive the data.

    ACct and ANdb share AOcr's field set, confirmed through AbilityMetaData's
    useSpecific column and WorldEditStrings:
      Ocr1 data1  Chance to Critical Strike (percent, 0-100)
      Ocr2 data2  Damage Multiplier
      Ocr3 data3  Damage Bonus          -- zero on every ability here
      Ocr4 data4  Chance to Evade (fraction, 0-1)
    Alucard's five hellhounds run 20%/x2 through 60%/x4. Itachi's A02W and
    Shiki's A066 are ANdb and are crit-plus-evade, not an orb: 5/8/10% and
    8/11/14% at x1.25/1.5/1.75, with 7/14/21% evade.

    Two things that would have been got wrong by building the obvious thing:
      - A051 블러디 어택 must be left alone. Its Ocr1 is overridden to exactly
        0, because the map does that proc itself in war3map.j on
        EVENT_PLAYER_UNIT_ATTACKED -- 50% for Alucard's agility -- and
        world.js already fires that event. An engine crit would double it.
      - Alit is not a damage ability at all. Its only two fields are Graphic
        Delay and Graphic Duration; there is no damage and no chance anywhere
        in it or in the map's overrides. It is a per-attack lightning strip,
        and world.js can already emit one. Inventing a damage figure for it
        would be inventing a mechanic.

    Aspo has one live carrier (Orochimaru's n00A) and needs machinery that
    does not exist: a per-buff damage-over-time tick, an attack-speed buff
    channel, and a slow that works on non-heroes. Its damage (4/s) and
    duration (5 s, 1 s on heroes) are solid; NOT ESTABLISHED is whether
    Spo2 0.5 and Spo3 0.25 are multipliers or reductions, and whether
    Spo4 = 1 is the enum index Movement or bitmask bit 0 Damage.

    The hook site is settled: world.js's `land` closure inside stepAttack, the
    one point both melee and missile attacks pass through, already hosting
    cleave and lifesteal, with attacker, victim and the damage roll in hand
    and no spell flag. Two constraints found with it -- recalc returns early
    on non-heroes and every carrier here is a summon, so the recalc-stash
    pattern used for lifesteal will not reach them; and auraEffects is
    imported by world.js and never called, so it is not a model to copy.

    DONE: ACct and ANdb, through attackProcs in abilities.js -- a fourth
    passive table beside ATTR_SKILLS, AURA_BASES and ITEM_BONUS, but read at
    attack time rather than stashed by recalc, because recalc returns early on
    non-heroes and every carrier but Itachi and Shiki is a summon. The
    multiplier goes on the swing inside stepAttack's `land` closure, the one
    point melee and missile attacks share, before damage() reads armour -- a
    crit is struck before armour, not after. Evade got a site at the same
    place: it has to stop the hit entirely, so it short-circuits ahead of
    cleave as well.
    A051 needed no special case in the end. Its chance is zero and zero cannot
    fire; the test pins that, so a later "fix" of the zero fails there rather
    than double-critting in play.
    The audit's dead list goes 13 -> 5 (ability_audit.mjs taught to count the
    new table). tools/proc_test.mjs, 20 checks, driving the real attack path
    with Math.random stubbed so the roll is a decision rather than a coin --
    and with the damage dice taken out, since the same stub feeds them and
    would otherwise be measured instead of the proc. A losing roll deals
    exactly the plain swing, a winning one exactly x4.000.

    NOT DONE, and neither can be built from the data:
      - Alit. Its only two fields are Graphic Delay and Graphic Duration.
        There is no damage and no chance in the ability, in the map's
        overrides, or in AbilityMetaData. It is a per-attack lightning strip
        and world.js can already emit one, but what the delay is measured from
        and whether the strip runs attacker-to-target are unstated, so even the
        graphic would be half invented.
      - Aspo. Damage per second (4) and duration (5s, 1s on heroes) are solid,
        but whether Spo2 0.5 and Spo3 0.25 are multipliers or reductions is
        not stated, and Spo4 = 1 reads as enum index "Movement" or bitmask bit
        0 "Damage" depending on how it is taken. It also needs a per-buff
        damage-over-time tick, an attack-speed buff channel and a slow that
        works on non-heroes, none of which exist. One live carrier.
    The plain evasion family went in with it, and the note that used to sit
    here calling A01W unbuildable was wrong. AbilityMetaData gives Eev1 to
    exactly AEev, AIev, ACev and ACes and WorldEditStrings reads it "Chance to
    Evade" -- the same label as Ocr4, one slot earlier. Its declared range is
    0..10, which does not on its own settle whether 0.25 means a quarter or a
    quarter of a percent; the map's own tooltip does, because A01W 맨트라
    carries 0.25 and its ubertip reads "◎피할 확률 25%". So it is a fraction
    here, like Ocr4. Live carriers: Eneru's god form (25%), Ichigo's 호로화,
    and Blizzard's own Demon Hunter and creep evasions.
    The audit's dead list is 4.

[x] Craters did nothing -- Kisame's water prison left the ground flat  (FIXED,
    with one approximated part, marked below and in the code)
    Reported from play. The map makes six TerrainDeform calls and four are
    craters: Gaara's A03O 사막의 수록 and Kisame's A069 개미 물지옥 each dig one
    and cancel it about ten seconds later. TerrainDeformCrater was
    `() => null`, so nothing moved.

    What the files establish exactly, and what the code now does:
      - the route. tft_supplement.j's native lines are NOT the hook: boot.js
        loads the supplement only when common.j is the Reign of Chaos one, and
        with the MPQs extracted it is TFT. The live path is war3map.j ->
        Blizzard.j's real TerrainDeformationCraterBJ -> common.j's native ->
        the engine. So the wrapper's arithmetic is the map's own script
        running, not something reimplemented: duration reaches the native in
        milliseconds.
      - the arguments. All four calls are literals and none scales with level:
        radius 400, depth +400 then -400 at the same point, 500 ms, permanent.
      - the direction, which decides whether a water prison is a pit or a
        hill. Blizzard states it rather than leaving it to be inferred:
        UI/TriggerStrings.txt's TerrainDeformationCraterBJHint reads "Depth may
        be negative for bumps", so a positive depth digs. That file is the
        World Editor's own help text, shipped inside the MPQ -- the same class
        of source that settled Ore1 for Reincarnation.
      - which vertices move. The client terrain is a real heightfield, one
        vertex per w3e grid vertex at 128 units, and render.js keeps the array
        it was built from. heightAt and pickGround both read it, so units,
        doodads, missiles and clicks follow the new ground for free.

    APPROXIMATED, and the only part that is: the radial falloff curve.
    TerrainDeformCrater is a bare `native` line in common.j and the shape it
    applies is in Blizzard's engine binary, which is not in war3.mpq or any of
    the three expansion archives -- the MPQs are data. render.js uses a raised
    cosine (full depth at the centre, nothing at the rim, zero slope at both
    ends) through a function named APPROX_CRATER_FALLOFF, with the reasoning
    on it. It is not claimed to be Warcraft III's, and settling it needs a
    measurement against the retail game rather than another reading of the
    files. Nothing else in the crater is a guess.

    What makes shipping the approximation safe: the map always cancels a
    crater with an equal and opposite one at the same point and radius, and
    displacement is linear in depth, so whatever curve is used the undo
    restores the original heights exactly. The error is bounded to how the
    hole looks for ten seconds; it cannot accumulate and dent the map.

    Server-side there is nothing to keep in step. The engine's own heightfield
    is read by exactly one native, GetLocationZ, and war3map.j never calls it
    (0 occurrences), so the deformation is the client's alone and no
    simulation state depends on it.
    tools/crater_test.mjs, 17 checks, asserting the heightfield's own numbers
    and the drawn mesh's vertex rather than that a function was called -- a
    stub passes the latter. The check that matters is the last one: dig, then
    undo, and the vertex has to come back to where it started to four decimal
    places.

    NOT DONE, deliberately:
      - Ripple. The other two of the six calls, on Rob Lucci's A00H and Zoro's
        A03H. Same transport, but its wave profile is unestablished the same
        way the falloff is, and unlike the crater it is never cancelled, so an
        approximation there is not self-correcting. FableAudit.TXT attributed
        these two to Yusuke and Rock Lee and was wrong; corrected there.
      - Temporary craters. The map only ever asks for permanent ones, and what
        `duration` means for a temporary deformation (time to dig? whole
        lifetime including the return?) is not stated anywhere.
      - The water mesh bakes its shoreline alpha from the heights at build
        time and is not rebuilt, so a crater dug into the moat edge will not
        move the waterline. Neither spell targets water in any observed cast,
        so this is noted rather than fixed.

[x] An empty order string was read as proof of a passive  (FIXED)
    Found by tools/ability_audit.mjs, not from play. isPassive returned on the
    order line and the targets line under it was dead code:

        if (ab.order !== undefined) return !ab.order;   // returned here
        const t = (ab.targets || '').toLowerCase();     // never reached

    The order string is the right signal in one direction only. It comes from
    Order= in Units\*AbilityFunc.txt, and Blizzard writes that line for only
    some abilities: Fire Bolt (Awfb), Finger of Death (ANfd), Cloak of Flames
    (AIcf) and Neutral Regen (ACnr) each carry an Art= line and no Order= at
    all. Measured: 462 of the 1046 abilities here have no order string, and the
    map casts two of them -- A00H and A01G both raise
    EVENT_PLAYER_UNIT_SPELL_EFFECT in war3map.j. So absence proves nothing, and
    isPassive now falls through to the targets the ability declares. Presence
    still decides, and the PassiveButtons icon still overrides both.

    Six verdicts move, and only one of them was doing harm:
      A00V  종경 백제검, Byakuya      5000 damage, Fire Bolt, no engine case and
                                     no trigger -- it did nothing at all, and
                                     now takes the data-driven damage fallback
      A01O  하늘의 쇠사슬            60 damage, Finger of Death, on h02I
      A01G, A00H                     the map's own triggers already ran them
      AIrl, AIpr                     potions; useItem never calls execute
    The audit's dead list goes 14 -> 13, the fallback list 18 -> 19, and nothing
    becomes newly dead.

    The dangerous half was the four that must NOT move. AIcf and ACnr carry a
    damage figure and an area, so reaching the fallback would have fired a
    500-damage nuke where the map wants a burn and a 40-damage one across 800
    units where it wants a heal. They are named in PASSIVE_BASES on the map's
    own use of them, not on the missing field: A05Y is handed to a
    three-second o007 dummy by SetUnitAbilityLevelSwapped and never ordered,
    A020 rides Eneru's Thunder God form and does not appear in war3map.j at
    all, and A002 sits on the base structure hatw and targets allies.
    tools/passive_test.mjs, 22 checks, asserting the victim's health rather
    than the predicate's return value.

    Two findings from the same measurement:
      - the map's own aart override is not a reliable passive tell, so it is
        deliberately not used. Two abilities here wear a PassiveButtons icon
        and only one means it: A057 아이스타임 has cooldown 2, range 800 and a
        five-second duration and is plainly active. The icon rule therefore
        stays keyed to Blizzard's base art.
      - tools/abilities.py did not read the map's own aord override, so an
        ability that renames its order string was compiled with the base's.
        A06D, the captured Monster Ball, is the only one: it answers to
        'summoning' where its AIrr base says 'roar'. FIXED -- the compiled
        table now prefers the map's field, and exactly one of the 1046 entries
        changed. No behaviour moves today, because nothing consumes the value
        of an order string: isPassive and itemSpell both only test whether one
        exists. That is exactly why it would have gone unnoticed until
        something did dispatch on the string, which is the reason to close it
        now rather than file it.

    NOT DONE: A020 and A05Y are correctly silent rather than correctly burning.
    Cloak of Flames damages on its own clock, and what the data carries is
    Area, the damage in Icfd (Blizzard 10; this map 500 on A020 and 850-3000
    across A05Y's five levels) and a duration of 1. Whether that duration is
    the interval between bursts is not stated anywhere in AbilityMetaData --
    adur is the generic Duration field every ability shares, so its label
    settles nothing the way Ore1's did for Reincarnation. Guessing it is a
    one-second pulse is exactly the kind of plausible inference that has been
    reverted here before, so the pulse is not written.

[x] The Monster Ball caught nothing  (FIXED)
    I006 몬스터볼, 1000 gold, one use, sold from a shop: aim it at a creep under
    30% life and there is a 30% chance it is yours. The map writes the whole
    mechanic itself across four triggers, and every one of them was landing on
    an engine that could not carry it.

    What the script does, verbatim, none of it reimplemented here:
      SPELL_EFFECT / 'A03V'  the target must be owned by Player(12) -- creeps
                             only -- and at GetUnitLifePercent <= 30. The roll
                             is GetRandomInt(0,'d') <= 30; 'd' is a one-char
                             literal, so it lexes to 100 and the odds are the
                             30% the tooltip prints. On success the creep is
                             hidden, paused, made over to the captor, and a
                             captured ball I008 is stamped with its index by
                             SetItemUserData.
      USE_ITEM   / 'I008'    a toggle: out 150 units in front of the holder,
                             unpaused and vulnerable, or back in.
      DROP_ITEM  / 'I008'    puts it back in regardless.
      PAWN_ITEM  / 'I008'    RemoveUnit -- selling the ball kills what is in it.
    All four find their creep the same way: loop 1..count comparing
    GetItemUserData against the stored index.

    Three separate gaps, each silent:
      - SetItemUserData/GetItemUserData were `() => {}` and `() => 0`. Every
        ball reported index 0, so no loop iteration ever matched and use, drop
        and sale all did nothing at all.
      - pawnItem fired PAWN_ITEM without a soldItem key, so GetSoldItem()
        returned null and the sale branch could not run even once the ids
        matched.
      - nothing could aim an item. useItem fired only USE_ITEM, with no target
        and no spell events, so the capture trigger's own condition -- which
        reads GetSpellTargetUnit -- was never reached.

    The third is the only one that needed a design decision, and it is one
    item's worth of behaviour rather than a system: of the map's 332 item
    types exactly one carries an ability with both an order and a unit target,
    and it is this ball. useItem now resolves that ability and fires the five
    spell events before USE_ITEM; the client arms a crosshair for an item the
    server marks `targeted` and sends the click's unit with it, and escape
    lets go of it the same way it lets go of a pending cast.
    tools/monsterball_test.mjs, 23 checks -- 17 through a real capture,
    release, recall and sale, 6 in the browser for the aiming.

[ ] A transformation's active abilities are on the unit but not clickable
    Fixed below: a metamorphosis now grants its alternate form's ability list,
    so Ichigo's Bankai really does carry the Black Getsuga (A030) and Eneru's
    Thunder God form carries its four. The passive half of that works -- the
    Super Saiyan attribute bonuses land -- but an *active* one has no slot,
    because sendHero and slotAbility both build the six slots from the hero
    definition's own castable list rather than from what the unit holds.
    Five of the sixteen morph forms grant an active ability this way; one of
    them (O003) grants four, which is more than q/w/e/r/d/f has room for.
    Entangled with the hotkey item below -- do them together.

[x] Hotkeys  (FIXED)
    The map assigns each hero ability its own key in war3map.w3a ('ahky'). The
    client used a fixed Q/W/E/R/D/F row, so on most heroes the letter printed on
    the button and the key that actually cast were different keys.

    The old note here was wrong twice about the data. It said all 130 abilities
    declare a hotkey -- 109 of the 130 do -- and it implied nothing compiled the
    field. compile_game.py:206 already emitted it (`hotkey=clean(lvl_get(m,
    'ahky', 0, ''))`) and had all along; abilities.py drops it deliberately as
    presentation the *server* never needs, which is correct. The only real gap
    was sendHero, which built the client's ability payload without it.

    Measured before touching anything:
      declared hotkeys .......... 109 of 130 hero abilities
      distinct keys ............. Q 24, W 24, R 23, E 21, T 7, B 4, V 4, C 2
                                  -- T, B, V and C were unreachable, and D and F
                                  are declared by no ability at all
      collisions ................ 0 heroes give two castable abilities the same
                                  key, so binding needs no tie-breaking
      slots with no hotkey ...... 16 of 125 castable (3 innate); they fall back
                                  to their position, and only to a letter no
                                  declared hotkey on that hero already holds --
                                  3 heroes need that guard

    Resolved once in main.js and written back onto the ability as `key`: the
    command card labels from the same field the keydown handler binds, so the
    printed letter is by construction the letter that casts. That single
    resolution point is the fix -- the bug was two places deriving the key
    independently.
    tools/hotkey_test.mjs, 9 checks, driven through a real match: Byakuya
    Kuchiki, whose six abilities are C Q W E B V, so every slot moves off its
    old position and a regression cannot pass silently.

[ ] SetUnitTimeScale -- 36 calls, currently a no-op
    Animation speed changes (hastes, slows, channel speeds) do nothing.

[ ] Text tags: no floating text renders at all
    Measured 2026-08-30, and this entry used to understate it as "text does
    not drift" -- the text never appears. CreateTextTag returns an inert
    handle (engine.js), nothing serialises a tag to the client, and the
    client's addFloater/floaters machinery has zero callers. Missing on
    screen: the engine's own bounty gold "+N" on kills (war3map.j sets
    GIVES_BOUNTY for all ten players and world.js already pays the gold),
    the map's 48 scripted tags (spell-name shouts over casters, the duel
    arena's 3-2-1-Fight countdown, "Winner is <hero>"), and only then the
    SetTextTagVelocity/Fadepoint/Permanent motion natives (26 calls each,
    no-ops) -- the drift of text that first has to exist. One feature, not
    several: a TEXTTAG message in the snapshot path, the client wiring
    addFloater to it, then the motion natives. Fuller breakdown in
    AbilityAudit.txt.

[ ] Fog of war
    Nothing exists. The whole map is visible to everyone.

PERFORMANCE
-----------
[ ] Skinning is the suspected remaining cost, unconfirmed on real hardware
    On a 4080 at 307 units the frame is 45 ms while the GPU has only 137k
    triangles and 1559 draw calls to draw -- so it is CPU, not the card.
    Freezing animation saves 29 ms with the draw calls unchanged, and only 9 ms
    of that is measurable in our own timers; the rest is three.js recomputing
    and uploading a skeleton per visible skinned mesh during render. Confirm
    with the in-game keys before optimising: ` for the readout, [ to freeze
    animation, ] to hide units.
    Ruled out already: merging geosets to cut draw calls. Only 8% of them are
    safe to merge -- the rest share a material but not visibility, and merging
    those breaks the animated geoset switching that Bankai depends on.

[ ] GPU texture handles are never released, and disposing them does not help
    Geometry and materials now come back on removeView -- 29 -> 55 -> 29 over
    three spawn/remove cycles, asserted in render_test. Textures do not:
    renderer.info.memory.textures climbs 9 per unit spawned and never falls,
    for the whole match.
    It is not a reference we hold. At rest between cycles the scene traverses
    to 2 meshes and 2 textures, flat, and exactly 3 Texture objects are created
    per spawn -- the texanim layer clones, which releaseGPU does dispose.
    The mechanism is three.js's own accounting (r185, WebGLTextures):
    info.memory.textures counts one entry per (texture.source, sampler-state
    key), where the key is getTextureCacheKey -- wrap, filters, anisotropy,
    colorSpace and friends. An entry is only freed when its usedTimes reaches
    zero, so disposing one texture that shares an entry moves nothing:
    measured, disposing a texanim clone changes the counter by 0.
    What has NOT been established is which 9 that is, given only 3 objects are
    born. The next step is to instrument getTextureCacheKey rather than guess
    -- the signature (monotonic growth, nothing retained) is what you get when
    a shared texture's key changes after it has been uploaded, which orphans
    the old entry forever. Look for a per-spawn write to a cached texture's
    wrap/filter/colorSpace.
    Worth keeping in proportion: the pixel data is uploaded once per source and
    shared, so this is handle growth rather than image growth. It is still
    unbounded, and it is the last thing standing between the ` counters and
    actually flattening.

[x] o00D has no entry in unittypes.json  (FIXED -- it never should have had one)
    The entry assumed a compiler gap. It is not one. A05N is a summon, not a
    morph, and it names a different unit per level: o000, o00D, o00E, o00F,
    o00B. The raw war3map.w3u contains none of the five in any case -- the
    map's own ids there are O000-O008 and one o007 -- so these are stale
    references the author left behind, and Warcraft III cannot summon a unit
    type that has no definition either.
    What was wrong was ours: world.summon called createUnit, which builds a
    unit even with no type record, so a nonexistent id became a real modelless
    unit on the client. summon now refuses an undefined type, which is what the
    game does. ghost_test reports "none" and `npm test` has no known-failing
    row left.

[x] tools/ghost_test.mjs prints raw format specifiers  (FIXED)
    Its console.log used %-6s and friends: Node's format understands %s and %d
    but none of printf's width flags, so the specifier printed itself and
    shifted every argument after it by one (hence the "xNaN"). padEnd instead.

SOUND
-----
[x] Warcraft III's own hero-death warning  (FIXED)
    "Our hero has fallen!" is the engine's, not the map's: nothing in
    war3map.j mentions it, which is why it had never been ported. UISounds.slk
    carries HeroDies<Race> for your own hero and AllyHeroDies<Race> for an
    ally's, all 11 rows already converted to ogg by the existing audio step.
    Three details that make it more than "play a sound on hero death": the
    voice is the *listening* player's race rather than the dead hero's, so the
    row is chosen per client and the server only reports the death; there is no
    enemy row in the table at all, so Warcraft III is silent when the other side
    loses one, and so are we; and it is not a 3D sound -- it plays at its own
    volume wherever the hero fell, because the voice is yours and not the
    battlefield's. 25 of the 26 heroes are race human and one is orc, so the
    race is read from unittypes rather than assumed.
    The rows also carry Flags, and every one of them is NODUPLICATES: Warcraft
    III will not start the warning while it is already playing, which in a map
    where heroes die constantly is the difference between a warning and a
    pile-up. Honoured. CHANNELFULLPREEMPT is deliberately not -- it decides what
    to evict when a mixer channel is full, and there is no channel model here.
    The premise was checked rather than assumed: mapmakers ask how to *remove*
    this sound, so it fires in custom maps by default, and FOC never suppresses
    it -- zero VolumeGroupSetVolume, zero SetSoundVolumeBJ, no VOLUME_GROUP_*
    anywhere in war3map.j. What is still unverified is the exact firing rule in
    a 10-player free-for-all; worth a listen in the real client.
    tools/warnsound_test.mjs, 18 checks, covering all four branches.
    Shipped broken first time and caught in play: the client passed
    `/assets/${file}` to playUI, but audio.load prefixes /assets/ itself, so the
    URL doubled, 404'd, and the game stayed silent. Every check still passed --
    they asserted that playUI was *called*. The test now drives a real hero
    death through a debug kill and asks the mixer whether the clip decoded,
    which is the only assertion that could have failed.

[ ] The rest of the UISounds warning family is still unported
    The same table drives TownAttack<Race>, AllyUnderAttack<Race> and a dozen
    others on the same pattern. Only the hero-death rows are compiled into
    data/uisounds.json so far. Nothing else is wired to fire them yet, so this
    is a "when there is something to warn about" item, not a gap in what plays.

SERVER
------
[x] Room lifecycle  (FIXED)
    Two bugs stacked. reset() kept players who had left during a match, so every
    finished match left its pair behind and after five the ten slots were gone.
    Then the fallback for a full room walked PLAYER_SLOTS, ran off the end, and
    used the loop *index* as the slot -- which at a full room is 10, itself a
    real slot on team 0 and already taken. Two late joiners both became player
    10 on team 0, and the duel they were meant to fight had one side empty.
    Now: reset() drops anyone with no socket, and a genuinely full room refuses
    the join instead of inventing a seat. tools/room_test.mjs drives twenty
    match cycles through one room and checks both.

[~] Ichigo's near-death transformation  (engine half FIXED, art half open)
    FIXED: he respawns, and the Hollow form grants its AGI x3 attack bonus.
    OPEN:  the buff's attached art -- see "Buff art" below.

FIXED -- metamorphosis (kept for the reasoning; delete once it has bedded in)
----------------------------------------------------------------------------
[x] An alternate form drew as the unit it transformed from
    Warcraft III has no native that changes a unit's model. A metamorphosis
    swaps the unit for another *type*, and where both types name one model file
    the difference is carried by Required Animation Names: the alternate type
    asks for "alternateex" and the model's geoset animations show a different
    body in its "... Alternate" sequences. Three things were in the way:
      - tools/unittypes.py never mapped 'uani', so a map that set the field
        itself lost it. O000 (Bankai) reached the client with an=null.
      - pickClip treated a unit's declared tokens as a tiebreak used only when
        nothing matched exactly, so a model carrying both "Stand" and "Stand
        Alternate" always drew the plain one. 57 unit/animation pairs across 22
        unit types were wrong: the Castle drew as a Town Hall, the Fortress as a
        Great Hall, the swimming penguin as a walking one, the Obsidian
        Destroyer as the statue, Illidan's demon form as Illidan.
      - "alternateex" names no sequence in any of the 1101 converted models
        while "alternate" names 214, and every unit type declaring it is the
        morphed form of another, so it asks for "alternate".
    Covered by tools/morph_test.mjs (needs FOC_DEBUG=1: Bankai is a level-10
    skill and the level-to-cap key is the only way there without playing).

[x] A metamorphosis left the form's own abilities behind
    world.morph swapped the stat block and nothing else, but the alternate type
    is where a transformation's power usually sits. Goku's three Super Saiyan
    forms are +50/+100/+150 to all attributes and granted none of it; Ichigo's
    Bankai carries the Black Getsuga. 16 forms are reachable through morph();
    none of them declares Locust, Invulnerable or Permanent Invisibility, so
    granting the list wholesale is safe. Hero abilities are deliberately not
    granted -- an alternate form declares the same ones so they carry across,
    and granting them would hand the player ranks they chose not to learn.

[x] Attribute skills did nothing until something else recalculated
    learnSkill changed u.abilities and never called recalc, so Attribute Bonus
    and its kin sat inert until the next level-up, item or buff happened to run
    one. Now every path that changes a unit's ability list recalculates.

[x] ATTR_SKILLS knew one of the 24 bases that can carry an attribute bonus
    AbilityMetaData.slk names them on the useSpecific column of Iagi/Istr/Iint.
    We had 'Aamk', so every transformation built on AIx2 granted nothing.
    Deriving the set from the data instead would have been wrong: AInv's data1
    is how many inventory slots it opens and AIcf's is a damage figure.

NETWORK
-------
[ ] No rejoin, only a reload
    A dropped socket now says so and offers a refresh, which is what the review
    asked for as a minimum. Actually rejoining means the server holding the slot
    for a moment rather than dropping the player on close, and the client
    replaying WELCOME and the next snapshot onto the world it already has. The
    room already knows how to evict a player with no socket, so the missing
    half is the grace period and the client-side re-attach.

HOUSEKEEPING
------------
[ ] Five tests are stale, not one
    sound_test, spell_test, match_test, wincond_test and victory_test all
    hardcode hero ids H004/E00T and the name "Saber". Neither id exists in this
    map's 26 heroes, so they die during setup ("tavern for Saber: undefined")
    before reaching anything they claim to test. Predates this work.

[ ] No licence on the repo.

NOT BUGS -- recorded so they are not re-investigated
-----------------------------------------------------
- Byakuya Kuchiki (H00N, war3mapImported\Byakuya2.mdl) has no team glow.
  The source MDX collapses geoset 3 -- the TeamGlow geoset -- into a
  0.034 x 0.031 x 0.008 speck while keeping a stale extent of radius 234.
  Its two bones carry no KGSC scale track, so nothing expands it at runtime.
  Geosets 6 and 8 are collapsed the same way. Warcraft III renders it exactly
  as we do: nothing. 1 of 243 models with a team glow is affected.
